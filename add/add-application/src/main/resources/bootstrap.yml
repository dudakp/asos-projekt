spring:
  application:
    name: "add-application"
  cloud:
    consul:
      host: consul
      port: 8500
      config:
        enabled: true
        prefixes: config/main
        data-key: data
        format: yaml
#        default-context: apps
